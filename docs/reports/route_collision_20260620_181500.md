# Route-Collision Triage Report

Run folder:

```text
runs/route_collision_20260620_181500
```

Command:

```powershell
python run_experiment.py --prime-limit 31 --control-samples 16 --timestamp route_collision_20260620_181500
```

## Guardrail

This report separates local artifact evidence from signature-level theorem
terrain. It does not prove any generalized Fermat or Beal case. A resolved
collision can route to external Sage/newform checks, but it cannot become a
proof claim or a promoted Beal candidate.

## Counts

- Signature collision rows: `19`.
- Resolved known mismatches: `5`.
- Still blocked mismatches: `0`.
- `terrain_dominates`: `12`.
- `mixed_needs_external_check`: `5`.
- `artifact_dominates`: `2`.
- `calibrated_route_candidate`: `0`.

## Route Matrix

| bucket | cases | interpretation |
| --- | ---: | --- |
| `correct_collision_resolved_to_external_check` | 5 | Local artifact collision resolved conservatively to Sage/newform follow-up. |
| `correct_theorem_terrain_route` | 5 | Known solved or descent terrain routed structurally. |
| `correct_external_sage_route` | 7 | Modular-method terrain routed to external checks. |
| `correct_artifact_demotion` | 2 | True subgroup artifacts remain demoted. |

## Resolved Collisions

The three requested known-case mismatches now resolve correctly:

| signature | artifact prime | collision | resolved route | why not promoted |
| --- | ---: | --- | --- | --- |
| `(5,5,7)` | 11 | `mixed_needs_external_check` | `needs_external_sage_check` | local artifact row does not globally demote modular terrain |
| `(5,3,5)` | 11 | `mixed_needs_external_check` | `needs_external_sage_check` | local artifact row does not globally demote modular terrain |
| `(5,4,5)` | 11 | `mixed_needs_external_check` | `needs_external_sage_check` | local artifact row does not globally demote modular terrain |

Two adjacent fourth-power bridge rows show the same artifact-collision pattern:

| signature | artifact prime | collision | resolved route |
| --- | ---: | --- | --- |
| `(4,5,5)` | 11 | `mixed_needs_external_check` | `needs_external_sage_check` |
| `(5,5,4)` | 11 | `mixed_needs_external_check` | `needs_external_sage_check` |

## Still Demoted

The true subgroup artifacts remain demoted:

- `(11,11,13)` -> `artifact_dominates` -> `artifact_like`
- `(11,11,5)` -> `artifact_dominates` -> `artifact_like`

## Interpretation

The resolver now distinguishes row-level artifact shadows from signature-level
proof terrain. This removes the false global demotion caused by one
artifact-prone local prime, while preserving the core safety rule: no candidate
is promoted from local sparsity alone.

All resolved collision rows remain external-check routes. They need Sage,
newform, conductor, or descent work before any stronger claim can be made.
