# Theorem-Terrain Calibration Report

Note: the remaining mismatch section in this report is superseded by
[route_collision_20260620_181500.md](route_collision_20260620_181500.md), which
resolves the `(5,5,7)`, `(5,3,5)`, and `(5,4,5)` artifact collisions to
external Sage checks.

Run folder:

```text
runs/theorem_terrain_20260620_174500
```

Command:

```powershell
python run_experiment.py --prime-limit 31 --control-samples 16 --timestamp theorem_terrain_20260620_174500
```

## Guardrail

This report routes signatures to known theorem terrain. It does not prove a new
generalized Fermat or Beal case. Known solved terrain calibrates the router; it
is not local evidence and not a proof claim.

## Counts

- Sweep rows: `2160`.
- Known/calibration cases: `19`.
- `theorem_terrain_route`: `5`.
- `needs_external_sage_check`: `9`.
- `artifact_like`: `2`.
- `known_case_mismatch`: `3`.
- `calibrated_route_candidate`: `0`.
- Remaining true mismatches: `3`.
- Sage available: `False`.

## Route Matrix

| bucket | cases | interpretation |
| --- | ---: | --- |
| `correct_theorem_terrain_route` | 5 | Known solved or descent terrain routed structurally. |
| `correct_external_sage_route` | 9 | Modular-method terrain sent to Sage/newform follow-up. |
| `correct_artifact_demotion` | 2 | Known subgroup artifacts demoted. |
| `true_mismatch` | 3 | Expected modular terrain collided with artifact-like local behavior. |

## Corrected FLT Terrain

The diagonal FLT-style cases now calibrate correctly:

- `(3,3,3)` -> `diagonal_flt_style` -> `theorem_terrain_route`
- `(4,4,4)` -> `diagonal_flt_style` -> `theorem_terrain_route`
- `(5,5,5)` -> `diagonal_flt_style` -> `theorem_terrain_route`
- `(7,7,7)` -> `diagonal_flt_style` -> `theorem_terrain_route`

This is the desired behavior. The local RSG layers do not need to rediscover
FLT; they need to avoid treating solved theorem terrain as a scanner failure.

## External-Check Routes

The modular-method-style cases remain external-check only. The highest route
prior rows are still `needs_external_sage_check`, not proof candidates:

| signature | terrain | output label |
| --- | --- | --- |
| `(3,3,4)` | `known_modular_method_shape` | `needs_external_sage_check` |
| `(3,3,5)` | `known_modular_method_shape` | `needs_external_sage_check` |
| `(3,4,3)` | `known_modular_method_shape` | `needs_external_sage_check` |
| `(3,5,5)` | `known_modular_method_shape` | `needs_external_sage_check` |
| `(7,7,4)` | `fourth_power_bridge` | `needs_external_sage_check` |
| `(4,7,7)` | `fourth_power_bridge` | `needs_external_sage_check` |

## Remaining True Mismatches

Three cases remain calibration debt:

| signature | expected route | observed terrain | strongest signal |
| --- | --- | --- | --- |
| `(5,5,7)` | `modular_method` | `artifact_prone_shape` | `primitive:sparse_unit_survivor` at `ell = 11` |
| `(5,3,5)` | `modular_method` | `artifact_prone_shape` | `primitive:sparse_unit_survivor` at `ell = 11` |
| `(5,4,5)` | `modular_method` | `artifact_prone_shape` | `primitive:sparse_unit_survivor` at `ell = 11` |

These rows should block promotion until the route is clarified. They may be
legitimate artifact warnings, or the terrain library may need a more nuanced
entry for modular cases with small-prime artifact shadows.

## Promotion Rule

No Beal or generalized Fermat candidate should be promoted from local sparsity
alone. A research route candidate must pass artifact controls, terrain routing,
and the relevant external modular/descent check before being treated as a
serious proof route.
