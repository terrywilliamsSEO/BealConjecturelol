# Candidate Dossier: `5-4-5`

## Route Snapshot

- Signature: `(5,4,5)`.
- Canonical signature: `4-5-5`.
- Current route label: `worth_human_modular_review`.
- Sage status: `completed` for the focused level-220 coefficient export.
- Known-case status: `generalized_fermat_terrain`.
- Terrain classification: `artifact_prone_shape`.
- Terrain route: `artifact_like`.

## Evidence

- Artifact-risk notes: collision=mixed_needs_external_check; artifact_rows=1; sparse_unit_rows=2; artifact-prone local primes: 11.
- Sparse unit rows involved: `ell=31` needs_modular_shadow_follow_up survivors=15; `ell=11` artifact_explained survivors=1.
- p-adic lift status: padic_descent_rows=0; unit_lift_rigid_or_collapsed_rows=0.
- Frey template confidence: `0.61`.
- Sage job path: `runs/sage_followup_20260620_183000/sage_jobs/sage_5_4_5.sage`.
- Sage result path: `runs/sage_followup_20260620_183000/sage_results/sage_5_4_5.json`.
- Trace/newform summary: level-220 coefficient rows are available for the focused audit; q=13 and q=17 did not close the local case gap, so the audit now checks q in `{3,13,17,41,61}`.

## Focused Eliminating-Prime Reduction Diagnostics

- Pairwise masks `AB`, `AC`, `BC`, and `ABC` are primitive-forbidden.
- Single masks `A_only`, `B_only`, and `C_only` remain locally possible through mod q, q^2, and q^3.
- For the displayed Frey template `E: y^2 = x(x - A^5)(x + B^4)`, the diagnostic layer records `v_q(Delta)>0` and `v_q(c4)=v_q(c6)=0` for `A_only`, `B_only`, and `C_only` at q in `{3,13,17,41,61}` when the invariant formulas are available.
- The safe branch classification is `multiplicative_reduction_condition`, not a theorem claim.
- The multiplicative congruence audit compares `a_q(f)` with `+/-(q+1)` modulo 5. The allowed residues are `1;4` for q=3 and q=13, and `2;3` for q=17, q=41, and q=61.
- The local-case closure score records unit survivors, single-mask survivors, missing coefficients/formulas, and unapplied level-lowering assumptions for each focused q.
- The best-prime ranking is written to `best_eliminating_prime_545.csv` and `BEST_ELIMINATING_PRIME_545.md`; it ranks by fewest surviving unit branches, fewest surviving single-mask branches, cleanest multiplicative coverage, lowest reliance on q=3, and human-review priority.
- The focused local labels remain conservative. If any q has all tracked local branches closed, it is only a `local_case_elimination_candidate`; if every q has a survivor or gap, the route remains `local_coverage_gap`. The route ceiling remains `worth_human_modular_review`.
- The cross-prime branch compatibility audit compares q in `{13,17,41,61}` across each level-220 newform. In the completed coefficient run, no level-220 newform has a compatible branch assignment across all non-q=3 focused primes, so the safe label is `cross_prime_elimination_candidate`.
- The q=3 exceptionality audit records that q=3 is good relative to level 220 because `3` does not divide `220`, but it remains the smallest focused good prime and carries a q=3 reliance penalty. Because the non-q=3 cross-prime audit also closes the tracked assignments, q=3 is classified as `q3_consistent_with_larger_primes`.
- The best route summary ranks `non_q3_cross_prime_closure` ahead of the q=3 single-prime route in the completed coefficient run. This is route evidence only and remains capped at `worth_human_modular_review`.
- The quantifier-safety audit verifies the stricter exists-prime-per-newform route. In the completed coefficient run, newform 0 has complete same-prime branch coverage at q=17 and q=41, and newform 1 has complete same-prime branch coverage at q=13. The aggregate label is `quantifier_safe_cross_prime_candidate`.
- The conditional theorem packet, assumption dependency graph, and adversarial review checklist are now generated as focused sidecars. They state the assumptions and review checks without raising `(5,4,5)` beyond `worth_human_modular_review`.
- The Frey-conductor audit computes `c4`, `c6`, `Delta`, and `j` for `E: y^2 = x(x - A^5)(x + B^4)`, then records the conductor and level-lowering checks still required by hand.
- The conductor-support audit keeps level `220 = 2^2 * 5 * 11` as a candidate level only. Primes 2 and 5 need exact local conductor checks, 11 needs a support justification, and primes dividing A, B, or C must be removed by a valid level-lowering theorem.
- The conductor-exponent model records generic multiplicative behavior for odd primes dividing A, B, or C away from 2, 5, and 11, but bad-prime exponents remain `needs_human_tate_check`.
- The level-220 provenance audit keeps the aggregate target at `level_220_heuristic_target` and flags the factor 11 as `level_11_factor_unjustified`.
- The ABC-prime removal audit flags `abc_prime_removal_gap` for primes dividing A, B, or C until a level-lowering theorem removes them from the final comparison level.
- The Sage conductor sanity script is generated only for formula checks and synthetic finite-field samples; it is not route evidence.
- The candidate-level discovery layer generates all levels `2^a * 5^b * 11^c` with `a in {0,1,2,3}`, `b in {0,1,2}`, and `c in {0,1}`, writes `sage_candidate_level_expander_545.sage`, imports `candidate_level_newforms_545.json` when present, and ranks levels without assuming 220.
- The GitHub Actions Sage follow-up workflow now runs the candidate-level expander after the level-220 coefficient expander and publishes `candidate_level_newforms_545.json` plus the derived import, trace-filter, and ranking files.
- If candidate-level data is missing, the safe label is `level_data_insufficient`; if surviving newforms appear, the route becomes `level_sensitive_route`; if multiple plausible levels also show trace pressure, the safe label is `multi_level_trace_pressure_candidate`.
- The conditional route validity score is currently `conductor_gap_blocks_upgrade`, not an upgrade beyond review.
- If any invariant formula is missing, the Tate stub downgrades the branch to `needs_human_tate_algorithm` and the local label remains `local_coverage_gap`.
- Missing coefficients become `coefficient_missing`; unapplied level-lowering congruence assumptions become `level_lowering_assumption_required`.

## Candidate Rows

- `known_case:bridge_5_4_5:mixed_needs_external_check:prime=11`

## Known-Case Links

- Candidate case IDs: `bridge_5_4_5`.

## Why This Is Not A Proof

This is not a proof: the Frey template is symbolic, conductor levels are heuristic, Sage output is route evidence only, and `contradiction_claim_allowed` remains false.

## Recommended Next Mathematical Check

Run the Tate algorithm / reduction analysis for the Frey curve at q in `{3,13,17,41,61}` under A_only, B_only, and C_only, justify that the multiplicative-reduction branches satisfy the level-lowering congruence `a_q(f) == +/-(q+1) mod 5` at those primes, run the candidate-level Sage expander across plausible levels, and prove the Frey-conductor/level-lowering package: Frey attachment, minimal conductor at 2, 5, 11 and primes dividing ABC, residual mod-5 irreducibility, exact lowered comparison level, newform exhaustion at that level, and good-prime trace comparison validity. In particular, resolve `level_11_factor_unjustified` and `abc_prime_removal_gap`.
